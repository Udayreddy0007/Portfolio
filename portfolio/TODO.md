# TODO - Fix portfolio so results can be seen locally

- [ ] Replace `catalog:` dependency specifiers in `portfolio/package.json` with real npm versions ("latest stable" policy).
- [ ] Verify `npm install` succeeds.
- [ ] Set env vars `PORT` and `BASE_PATH` and run `npm run dev`.
- [ ] Confirm the site loads in browser (/) and routes work.

